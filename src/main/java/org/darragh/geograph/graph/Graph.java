package org.darragh.geograph.graph;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class Graph {
    /** Graph logger. */
    private final static Logger LOGGER = LoggerFactory.getLogger(Graph.class.getName());
}
